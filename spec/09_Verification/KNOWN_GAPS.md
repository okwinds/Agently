# 09 / 已知缺口（Known Gaps）

本规格文档以 `695c3140008830cea87021512770cfe464448085` 为基线（代码事实源），并通过“源码 + tests（可执行规格）”对齐到可复刻实现的粒度。

截至当前版本，本 spec **无明确已知缺口**（即：不存在“无法仅凭 spec 复刻当前行为”的模块）。如果你在复刻过程中发现与本 spec 不一致的行为，应以当前基线 commit 的源码为准，并将差异记录为新的缺口条目（这意味着代码已经变化或 spec 需要同步更新）。

说明（不属于“缺口”，而是复刻边界必须理解）：

- 任何与外部 LLM 供应商相关的行为（回答内容、工具调用意图）都不可被规格固定；本 spec 固定的是 **请求构造、流式事件映射、结构化解析、重试与工具编排机制**。
